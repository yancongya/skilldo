# 2026-08-28

- 完成 explore sources 功能审查并提交 commit `c4296bb`
  - 新增 15 文件改动，953 插入 / 215 删除
  - 修复依赖缺失（npm install），lint / build / rust fmt / clippy / 102 测试全部通过
- 启动 Vite dev server 并用 agent-browser 截取探索页预览
- 深入分析 `src-tauri/src/core/explore_sources.rs`，整理出实现要点与潜在改进点
- 落实 6 项优化并提交 commit `5d81cc8`（8 文件，+599/−86）
  - explore_sources.rs 并行拉取来源（std::thread）+ 跨来源去重
  - 新增 ExploreFetchResult（skills + errors），前端 ExplorePage 警告 banner 展示失败来源
  - git_index 来源：git clone/pull 仓库后读取内置 index 文件
  - 复用 core::expand_home_path（从 commands 私有函数提升）
  - normalize_source_value 仅对 owner/repo 两段短链接补 GitHub，避免误判 GitLab 等
  - 补 7 个单元测试（解析/去重/错误上报/归一化）
  - 验证：lint / build(tsc) / rust fmt / clippy(0 warning) / 109 测试全通过
- 调研 CLI 化可行性（老板想让 agents 通过 CLI 读/操作 Skills Hub，无需打开桌面客户端）
  - 找到参考仓库 HKUDS/CLI-Anything（agent-native 范式：JSON 输出 + SKILL.md 自发现）与 suchlab/anything-cli（Rust+clap，技术栈一致）
  - 结论：可行且优于 web 化；`core/` 解耦使新增 `cli/` 壳几乎零改动复用全部逻辑；CLI 与 Tauri GUI 互补
- 实现 skillhub CLI 首期（只读子集）并提交
  - 新增 src-tauri/src/cli/mod.rs + src/bin/skillhub.rs（clap 子命令 list/status/explore/sources list）
  - core::skill_store::default_db_path_cli 复用 dirs + app identifier，与 GUI 共用同一 SQLite（实测读到了 GUI 真实数据）
  - 全局 --json 结构化输出；SKILL.md 供 agent 发现
  - 验证：cargo clippy(0 warning) / fmt / 110 lib 测试全过；手动验证 list/status/sources/--json/explore 均正常
  - 二期：完整 AppHandle 解耦后补 install/sync/update/delete 等写操作
- 修复 CLI 退出码问题并提交 commit `317c220`
  - 坏 db 路径（目录当文件/不存在的深层目录）之前 exit=0（错误被吞），改为 exit=1
  - 改动：`ensure_schema().context(...)` → `ensure_schema().with_context(|| path)`，错误自然传播到 main 的 `process::exit(1)`
  - 验证：正常路径 exit=0 无回归，坏路径 exit=1 + 清晰错误信息
- CLI 一期完整测试报告（13 项）：help / list / list --json / status / status --json / sources / sources --json / explore --query / explore --json（含 errors 字段）/ --db 空库 / --db 坏路径，全部通过
- CLI 二期：core 层 AppHandle 解耦 + CLI 写操作子命令
  - commit `ba591b9`（core 层，742 行新增）：
    - central_repo::resolve_central_repo_path_cli（无 AppHandle 的路径解析）
    - installer 新增 7 个 `_cli` 函数：install_git/local_skill_cli、sync_skill_to_tool_cli、check_managed_skill_update_cli、update_managed_skill_from_source_cli、delete_skill_cli、unsync_skill_cli
    - skill_store 新增 find_skill_target 查询
  - commit `48cf9c9`（CLI 层，392 行新增）：
    - 新增子命令：install --url [--name] [--yes] / sync --skill --tool / unsync --skill --tool / update --skill [--all] [--yes] / delete --skill [--yes]
    - resolve_skill_id 支持按名称或 UUID 模糊查找
    - 所有写操作支持 --json 结构化输出 + --yes 跳过确认
    - 所有验证通过：cargo check / fmt / clippy(0 warning) / 110 测试全过
- 修复 install_local_skill_cli 重复安装冲突并提交 commit `2b20255`
  - source 已在 central repo → 跳过复制
  - DB 已有同 central_path 记录 → 复用而非新建（避免 UNIQUE 约束失败）
- 注册 skillhub skill 到 agent 发现机制
  - SKILL.md 安装到 ~/.skillshub/skillhub/
  - 通过 CLI install + sync 同步到 13 个 agent 工具（claude_code/codex/opencode/gemini_cli/cline/augment/openclaw/iflow_cli/kiro_cli/pi/qoder/qwen_code/antigravity）
  - 全部 symlink 成功，agent 开新会话即可发现 skillhub 命令
- 配置 shell function（~/.zshrc）：任意目录可直接调用 `skillhub`
- 项目根目录添加 `./skillhub` wrapper script
- 新增 local skill 自动 git pull + push 功能并提交 `d21594f`
  - update_managed_skill_from_source_cli: local 技能 sourceRef 是 git 仓库时自动 git pull
  - 新增 `skillhub push --skill <name> [--message]` 命令：对 central_path 有 .git 的技能执行 add/commit/push
  - 修复临时值借用错误（format! + unwrap_or）
  - 推送到 origin/main
- 完成构建脚本 scripts/build.sh 并提交 `97a9c8a`
  - 支持 dmg/universal/win/linux/cli/release 六种目标
  - 构建产物自动复制到 out/ 并清理中间文件
  - 执行一次 release 构建验证（前端 5s + Rust 3m15s），.app 生成成功
- 更新 SKILL.md 覆盖全部 11 个 CLI 命令并同步到 13 个 agent
- 修复 Tauri 构建兼容性：Cargo.toml 加 default-run + 显式 [[bin]] app
- 待办：项目文件清理（CLAUDE.md/CODE_OF_CONDUCT/CONTRIBUTING/SECURITY/.omo/docs 评估）
