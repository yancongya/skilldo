# 2026-08-30

## 收尾：config.rs 常量集中化（提交 afa855c → origin/main）
- 上一轮建了 `core/config.rs` 但编译没验证：宏 `concatcp!` 先于定义被调用且只支持 2 参数（实际传 3 个），导致 5 个编译错误。
- 修复：把 config.rs 改回纯字面量常量（GITHUB_OWNER/REPO/REPO_PATH、APP_IDENTIFIER、CENTRAL_DIR_NAME、GIT_CACHE_DIR_NAME、FEATURED_SKILLS_URL、UPDATER_URL），删掉坏宏。
- 另修 `cache_cleanup.rs:8` 的 `GIT_GIT_CACHE_DIR_NAME` 重复前缀 typo（应为 `GIT_CACHE_DIR_NAME`）。
- `cargo check` 通过，`cargo test --lib core::config` 2 测试通过。8 文件 +69/−14 提交并 push。
- 教训：Rust 里 `concat!` 只接受字面量（不接受 const 项），跨文件拼 URL 用字面量最稳；宏必须在使用前定义。

## 项目更名 skilldo 并完成脱离 fork（提交 16f67ab）
- 仓库/包名 skills-hub→skilldo；CLI 命令 skillhub→skilldo；显示名 Skills Hub→SkillDo，全部集中到 `core/config.rs` 的 PRODUCT_NAME/CLI_NAME。APP_IDENTIFIER 与 .skillshub 保留不变（避免分裂 DB 路径）。
- cargo test --lib 112 项全过。
- 新建非 fork 仓库 yancongya/skilldo，push 全部 11 分支 + 11 tag；旧 fork yancongya/skills-hub 归档（archived=true）。
- 坑：zsh 里 `$b:refs` 会被解析成 `$b` 的 `:r` 修饰符（rootname），变量要加花括号 `${b}:refs/...`；`grep -v '->'` 因 `->` 被当选项报错，改用 `git for-each-ref`。

## 脱离 fork 收尾（本会话续）
- 旧 fork `yancongya/skills-hub` 从「归档」升级为**彻底删除**（`gh repo delete`，已验证 404）。
- **重装本地 agent 发现 skill**：旧 `~/.skillshub/skillhub/`(name=skillhub-cli) + 软链 `~/.codex/skills/skillhub`、`~/.claude/skills/skillhub` 全部替换为 `~/.skillshub/skilldo-cli/`(name=skilldo-cli，内容取自仓库 SKILL.md，0 处 skillhub 残留) + `skilldo-cli` 软链。
- **关键坑**：重命名 CLI 二进制后，`~/.zshrc:27` 的 `skillhub()` shell 函数会断链（它写死 `target/release/skillhub` 与 `cargo run --bin skillhub`）。已改为 `skilldo()` 指向新二进制；这是 agent 真正调用 CLI 的入口，不只改发现 skill 内容。
- 清理 `target/` 下残留旧二进制 `skillhub`（debug/release 及旧 `Skills Hub.app` 包），下次构建再生为 `skilldo`/`SkillDo.app`。
- 端到端验证：`cargo build --bin skilldo` 通过；`skilldo --help` 显示 "SkillDo CLI" 且命令名从二进制派生为 `skilldo`。
- 注意：本地**工作目录**仍叫 `skills-hub`（仅目录名，不影响功能），zsh 函数与 git 仍指向它；如需彻底一致可另 rename 目录（未做）。

## 统一配置接口 + Settings 功能补全（提交 5ba1e73）
- 新增 `core/app_config.rs`：`AppConfig` 聚合所有设置（language/storage_path/git cache/github_token/origin_rules/tool_dir_overrides/custom_scan_dirs/explore_sources/exported_at），`CONFIG_VERSION=1`，`export_config_json`/`parse_config_json`（校验 version 与 source id/name 非空）。schema 放 core、聚合/落盘编排放 `commands/mod.rs`（core 不能依赖 commands）。
- 新增 Tauri 命令并在 `lib.rs` 注册：`get_app_config`/`save_app_config`/`export_config`(加 exported_at unix 秒)/`import_config`/`validate_github_token`(reqwest::blocking GET /user + 读 x-oauth-scopes)/`write_text_file`/`read_text_file`（避免引入 plugin-fs）。
- SettingsPage 新增四块：① 技能源管理（增删改/启用切换）；② GitHub token 校验徽章（login+scopes）；③ 工具状态概览表；④ 配置导出/导入备份（导入后刷新 githubToken/originRules/customScanDirs/exploreSources/toolStatus 局部 state，避免整配置回填覆盖 token）。
- 零迁移：`save_app_config_impl` 逐 KV key 写回，旧 DB 兼容；days 钳制 0..=3650、ttl 0..=3600、token trim。
- 踩坑：`@tauri-apps/plugin-dialog` 在 bundler 解析下 d.ts 不暴露 `save`/`filters`（TS2305/TS2353，磁盘 d.ts 实际有），改用动态 `import()` + 本地 `DialogModule` 接口 cast（`as unknown as`）；handler 需放在 githubToken/originRules 声明之后避免 TS2448。
- 顺手修 clippy（阻塞 `npm run check`）：`cli/mod.rs` 冗余字段名、`installer.rs` `unwrap_err`→`match push_result { Ok(()) => .., Err(e) => .. }`。
- `npm run check` 全绿（eslint / tsc -b / vite build / rust:fmt:check / rust:clippy / 112 tests 全过）。
